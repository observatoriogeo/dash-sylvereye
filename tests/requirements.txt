# Packages needed to run the integration tests.
# Switch into a virtual environment and:
#   pip install -e .
#   pip install -r tests/requirements.txt
#
# Selenium 4.6+ ships with Selenium Manager, which auto-downloads a
# ChromeDriver matching the installed Chrome — no manual driver setup
# needed. Chrome itself must be on PATH (apt: google-chrome-stable).
#
# Selenium is pinned past dash[testing]'s <=4.2 ceiling because we drive
# Chrome directly rather than via dash_duo.

dash>=4.0.0
osmnx>=2.0
numpy
pytest>=7.0
selenium>=4.20
